# 生产资金展示发布（2026-09-18）

## 范围和实现

用户要求线上不显示测试资金、测试钱包和测试数据。源码 `8c311ea5a8e16bd4e17af137da640b65f8461fd2` 已推送 main，保留同期卡片同步改动。首页和资金中心只读正式账本；缺失账户显示尚未开通，核对不一致不显示可用余额。前后台移除测试入口，旧路由跳转正式页面，网关与API以 `FUNDS_DISPLAY_MODE=production` 屏蔽测试接口。历史测试记录保留独立范围，不删除、不迁移余额。

## 发布证据

- Render API `srv-daepgj8u01pc73fgdhsg`，部署 `dep-damld767bikc73c3knjg`，源码上述提交；2026-09-18 15:21:52 UTC 状态 live。
- Cloudflare 客户端版本 `af4f41a7-8ca4-49c2-88fa-e330cdeef355`，后台版本 `b3bf584e-a545-4051-907d-60e8ad11c6cd`；均发布成功，保留原远端变量。
- 线上 healthz/readyz 均200。源站旧测试钱包404；客户测试钱包/资金404，后台旧测试API404；旧客户/后台页面302至正式页面；正常客户crypto未认证401。
- www与admin线上HTML引用的入口JS/CSS与本地发布产物逐字节SHA256一致。

## 验证与边界

本批132项前端/网关测试通过；Go隔离PostgreSQL race测试、go vet通过；两端类型检查及构建通过。覆盖正式余额读取、未知USD、shadow拒绝、跨客户拒绝、只读模式拒绝金融写操作，以及测试接口屏蔽。

浏览器实际打开正式客户端，正常进入登录页；无客户登录会话，未宣称登录后的真实余额页面浏览器验收通过。既有0.1 USDT充值在此前验收中已完成正式入账，本批未再次转账、未重复记账，也未重新执行真实渠道金额验收。

本批不执行迁移，不提高1 USDT限额，不启用提款、OTC或卡片充提。生产展示不等于全部金融能力启用。

回退：可回退本批应用产物，保持生产数据范围及测试API关闭配置；不得迁移测试余额或以模拟数据填补正式账户。若回退API使正式查询不可用，页面应保持不可用提示，不能回退到测试钱包。

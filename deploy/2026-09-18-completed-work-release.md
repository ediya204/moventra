# 已完成改动统一发布清单

日期：2026-09-18。范围由用户明确为“只发布当前已完成的改动”。本清单记录发布内容和本地验证，线上完成情况以部署平台记录为准。

## 内容

以 `f45100a` 的最新远程 main 为基线，保留此前已发布的客户隔离、测试快照、测试资金和 Slash 普通 webhook 接收能力。补入已经完成但尚未提交的商户 Logo 修复：客户端交易列表、卡片关联交易和交易详情使用共享商户图标组件；图片失败保留商户文本，Logo 请求不包含原始订单标识。

并行开发中的 Slash 来源层、生命周期及 APEXIS Op 钱包调整不纳入本次发布。没有新增数据库迁移、真实资金写入或账本启用。

## 本次验证

- 冻结锁文件安装成功，客户端与后台 TypeScript / Vite 构建成功；后台保留既有大 chunk 提示。
- 97 项前端、网关及契约测试通过。
- 17 项官网 SEO、构建产物及联系表单测试通过；联系表单测试使用测试替身，不发送真实邮件。
- 隔离本地 PostgreSQL 的 Go race 回归通过；没有连接生产数据库。

## 版本一致性

将包含本清单的最终提交快进推送至 GitHub main，再将相同提交部署到客户端 `moventra-web`、后台 `moventra-admin` 和 Render `moventra-api`。Cloudflare 版本标注提交 ID，线上入口产物与本次构建比对；Render 以指定 commit 部署并核验 live 状态和 readyz。自动部署仍关闭。

不在部署后追加仅文档提交，避免 main 比线上版本多一个提交。平台部署 ID、最终提交与线上探测结果随本次交付提供，不以本清单预先宣称部署成功或本人认证后业务验收通过。

## 本地整理

清理 7 条已失效的 Git worktree 登记和根目录 `.DS_Store`。过期的未跟踪 webhook readiness 文档已移入仓库外私有备份，现行记录为 [Slash webhook 上线记录](slash-webhook-online-2026-09-18.md)。保留正在使用的工作树及未完成代码；构建依赖、缓存和私有数据不提交。

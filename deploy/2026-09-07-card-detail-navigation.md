# 卡片详情导航发布

2026-09-07。用户授权同步 GitHub 并部署；基于 `26bdc4d` 独立发布工作区，保留主工作区的无关改动。

## 范围

- 正式后台交易抽屉使用站内导航打开关联卡片，避免整页刷新丢失内存登录会话；保留连接范围。
- 同步本地卡片列表、交易抽屉的独立详情入口，以及已有卡片工作台所需前端源码与卡内交易 GET 白名单。
- 同步此前已验收的本地可用余额展示列；无逐卡资金数据时显示“未接入”。
- 本地工作台仅 DEV 加载；未将本地 Node 服务、私有数据、内部管理动作部署到生产。

## 验证

- 两端 TypeScript/Vite 生产构建通过；现有图表大包提示保留。
- `pnpm test`：54 项通过，包含 2 项新增正式卡片导航回归。导航回归使用实际页面和 React Router，认证及读接口使用测试替身。
- 后台 Worker Wrangler 4.129.0 dry-run 通过，沿用已批准的运营邮箱构建配置。
- 之前本地 8850 浏览器已逐一验证卡片列表详情、交易关联卡及卡内交易标签；此项为前一轮本地证据。

## 发布状态

待本次发布完成后补充 GitHub 提交、Worker 版本及线上验证结果。

只发布后台 Worker，不发布客户端、Render API，不执行数据库迁移、权限变更或真实资金动作。正式后台没有本地“全部卡片”列表；线上详情仍是已授权渠道资料查询，不代表本地完整管理工作台已上线。

回退目标：后台 Worker `2f7ca654-ec7c-476f-adf9-207e157d4803`。

# 统一资金记录第二批发布 — 2026-09-19

## 授权、范围与基线

用户授权按顺序完成剩余任务并同步GitHub main、部署。协调任务明确交给本任务唯一发布权；实现任务已idle并停止写入/预览。基线main为67dedf5，本批34份实现文件与[交接清单](../docs/business/fund-records.md#最终交接文件范围)完全一致，无其他差异。

客户端 `/portal/fund-records`、后台 `/finance/fund-records` 统一查询持久非消费业务记录，支持筛选分页、独立详情、金额拆项与证据关联。客户所有权、运营MFA与各来源授权分别校验；记录业务状态与入账状态独立。真实渠道原始数据、账本权威与金融执行流程不改变。

本批没有迁移或新执行开关；只部署API及两端Worker，不重新发布未修改的issuing-worker。消息021、统一开卡022生产迁移和执行激活不在本批。未调用真实金融写接口。

## 发布前验证

本发布任务重新执行：191项前端/网关回归全部通过；两端TypeScript/Vite构建、全套隔离PostgreSQL race、Go vet/build、docs:check与diff --check通过。两端Wrangler dry-run通过。保留既有大chunk提示；需要外部Blnk/Firebase或专用浏览器环境的Go测试跳过，不宣称全真实端到端。

交接任务另已验证合成页面筛选/跨页/详情刷新返回/故障恢复/390px、删除021/022结构兼容、funds_cards.id及充值evidenceRef专项。本发布任务未复做其浏览器合成验收；真实登录验收不能由这些测试替代。

## 平台与上线核验

运行源码 `b1b27fbe311d39ec10a12fffede7641ff4394409` 已推送GitHub main。

- Render API：`dep-dampcdid0e5s73d82nk0`，2026-09-18 19:53:04 UTC live，commit与上述运行源码一致。
- 客户端Worker：`ec05446f-d05e-4eab-b25a-29d9f34062ea`；后台Worker：`5e0e0c2b-6c97-4fcf-a5ee-849548bc8134`。Wrangler 4.129.0保留既有vars发布。
- 线上客户端29份、后台45份JS/CSS与最终本地构建SHA-256全部一致。
- 上线后readyz200；API直连及两端对应资金记录GET未认证均401；两端访问另一角色路径404；对客户端只读资金记录POST为404。
- 两个资金记录页面HTTP200；浏览器打开后分别转到客户端/运营登录页并正常渲染。没有将静态页面200或负向鉴权当作真实业务验收。
- API上线后的error级日志查询无返回记录。工作区在源码发布后无差异；文档收尾单独提交。

尝试查找既有已登录会话：本任务内置浏览器无现存标签，Chrome连接无法获取标签；没有创建凭据或绕过MFA。真实认证后的资金列表/详情查询暂未执行。

分页采用每次请求独立快照的offset方式，新业务插入后连续页可能移动；未进行真实大数据量压测。

## 回退

API回退运行源码8cfc176；客户端Worker回退4d694e4f-8d06-4b77-84ca-6f01725837d2，后台回退6bd384f0-eec1-4664-950b-e25b02dd6dd9。回退程序及导航即可，不删除生产记录、不回滚数据库。操作前核对是否已有后续发布。

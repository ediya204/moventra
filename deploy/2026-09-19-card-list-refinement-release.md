# 卡片列表精简发布（2026-09-19）

用户授权同步 GitHub main 并部署。基线 `1b54673`，复用干净的 card-center-release 工作树；共享开发目录保持原状。本批仅包含客户端来源控件删除、卡名与尾号拆列、尾号详情链接及“详情”文案。

接口、后台、API、数据库、金融写入均无本批变更。余额和近30天消费的数据接入方案尚未实施，不纳入本次展示发布。

## 验证及发布

独立发布快照138项前端/网关回归通过，客户端TypeScript/Vite生产构建、文档检查、diff检查与Wrangler dry-run通过。浏览器自动化连接返回非JSON响应，本轮未完成新版本登录业务验收；不将历史登录验收作为新版本证据。发布结果如下。

## 回退

客户端上一发布版本 `0b1d8835-b5ae-44db-aa69-3ec5d2c348df`；本次部署前已用Wrangler deployments list核对。

## 实际发布结果

- 运行源码 `ef3fd48` 已推送 GitHub main。
- Cloudflare 客户端 `moventra-web` 版本 `334bc10c-b291-4c6c-94b0-a6677e273689` 已部署至 moventra.me、www.moventra.me 及原过渡域名。
- `/portal/cards` HTML入口与构建一致，全部26份JS/CSS线上与本地构建逐字节一致；curl核验客户端未认证me为401、跨端admin-api为404、API readyz为200。Python默认HTTP客户端一度返回403，改用curl完成上述检查；不将该403解释为业务授权结果。
- 浏览器自动化桥接异常，本批认证后视觉及点击验收未执行。自动化回归覆盖既有详情跳转、筛选与返回上下文，HTTP和资源核验不替代真实登录验收。
- 后台Worker、Go服务、数据库未执行本批部署或迁移，没有渠道写入或资金动作。原共享工作区未重置、未暂存；本次发布使用独立工作树。

# 卡片列表精简发布（2026-09-19）

用户授权同步 GitHub main 并部署。基线 `1b54673`，复用干净的 card-center-release 工作树；共享开发目录保持原状。本批仅包含客户端来源控件删除、卡名与尾号拆列、尾号详情链接及“详情”文案。

接口、后台、API、数据库、金融写入均无本批变更。余额和近30天消费的数据接入方案尚未实施，不纳入本次展示发布。

## 验证及发布

独立发布快照138项前端/网关回归通过，客户端TypeScript/Vite生产构建、文档检查、diff检查与Wrangler dry-run通过。浏览器自动化连接返回非JSON响应，本轮未完成新版本登录业务验收；不将历史登录验收作为新版本证据。实际提交、Worker版本和线上资源核验在发布后补充。

## 回退

客户端上一发布版本 `0b1d8835-b5ae-44db-aa69-3ec5d2c348df`，实际部署前再次核对。

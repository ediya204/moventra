# 卡片中心布局与状态发布

用户明确要求检查数据连通性、同步代码并部署。2026-09-19；基线main `14fe215`，使用独立card-center-release工作树，只纳入卡片中心列表、开卡导航位置、双端共享状态标签及对应测试文档。共享目录仍有其他任务修改详情/CVV、首页和通用布局，本批不纳入。无API代码、迁移、金融状态或权限变更。

## 交付范围

- 顶部申请新卡/开卡订单；关键词和状态服务端组合筛选、条件清除、分页及详情返回上下文；当前页排序。
- 桌面列表和移动端卡片，状态浅色标签，客户列表“状态”表头；创建日期YYYY/MM/DD。
- 余额/近30天消费列目前仍为“暂不可用”：正式DTO没有可靠逐卡余额或完整30天汇总。发布UI不等于已接通金额，不把钱包余额、额度、当前页交易或旧导入当作此指标。
- 保留线上现有卡片详情、确认机制及交易查询，不引入正在开发的CardDetailWorkspace或CVV接口。

## 发布前本次验证

- 独立快照138项前端/网关测试通过，两端类型检查通过；与共享目录142项的差异为未包含另一任务新增4项首页测试。
- Chrome现有客户登录会话：本人23张授权卡列表可读，一张卡详情及1条关联交易可读；没有点击启用、停用、注销或向渠道核对按钮。
- healthz/readyz为200；客户端/后台me无认证401，跨端API404。此6项检查仅证明基础服务和入口边界，不替代认证业务读取。
- 构建、dry-run及实际部署版本继续追加在下方。API无需本批部署，数据库和真实金融写调用均不执行。

## 回退与共享目录

发布前客户端版本9b46c180-be03-4102-9e0f-d454b7cd47f4；后台版本4a9530e1-de90-4142-847a-e796ed6acd66。按对应Worker版本回退，不回退数据库或金融状态。原共享工作区保留全部并行改动，本批仅从独立提交推送origin/main。

两端生产构建、Wrangler dry-run通过；后台保留既有大包提示。文档链接/目录与diff检查通过。

## 实际发布与上线核验

- 源码 `0ea9ac9a7f445645816773c401485af570e3c185` 已推送GitHub main。
- 客户端moventra-web版本 `0b1d8835-b5ae-44db-aa69-3ec5d2c348df`，正式/原过渡域名发布成功。
- 后台moventra-admin版本 `9a420f9e-e9eb-40bf-9efb-6ac2d3fdae23`，正式/原过渡域名发布成功。
- 两端HTML入口引用与9份相关JS/CSS和独立构建逐字节一致。发布后API readyz 200，两端未登录me为401、跨端404。
- 新版Chrome真实客户登录：重新通过已有Google会话登录后，列表23张/首页20张；状态paused筛选9张、组合尾号搜索1张。新状态标签、“状态/余额/近30天消费”列及YYYY/MM/DD日期可见。余额和消费列仍无真实数字，不属于金额接通验收。
- 本批上线后未完成后台本人MFA业务验收；卡片详情和关联交易在发布前现有登录会话已确认可读，发布后筛选查询已确认；没有进行真实金融写入。API与数据库没有本批部署/迁移。
- 共享目录后续仍有并行开发，本次没有重置其HEAD、索引、未提交或未跟踪文件。发布源码及证据在独立工作树保存，origin/main为同步依据。

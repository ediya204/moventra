# 交易、订单与资金领域规范

当前能力见[状态摘要](../current-state.md)。下方 DESIGN 模型及 F/R 场景不是已实现或全部通过的能力；本地隔离业务源码已恢复至 [services/local-workspace](../../services/local-workspace/README.md)，私有数据与凭据未迁入。后续日期的实施补充保留其独立证据，不代表本次重跑测试。

更新日期：2026-09-07。状态：DESIGN；以下名称为内部模型，不是 Slash 原生字段或已完成数据库表。

渠道事实见 [Slash 接入规范](../integrations/slash.md)，接口见 [契约](../api/contract.md)。

## 1. 资金事实的权威来源

| 数据 | 权威依据 | 不得替代为 |
| --- | --- | --- |
| 渠道余额 | 已核验账户、类型、币种及时点的渠道快照 | 交易列表简单求和、卡片限额 |
| 内部客户资金归属 | 原业务账本及生效映射 | Slash accountId、登录账号或商户订单号 |
| 实际入账资金流 | 有证据的入账交易/账本记录 | 授权申请、pending 金额、退款意图 |
| 经营分析 | 明确口径的派生投影 | 真实可出款余额 |
| 对账结果 | 可比范围的事实及差异证据 | 为配平而编造的调整 |

当前本地卡预算、Demo 钱包与 Slash 来源账户分开；不得把已在来源账户体现的历史消费再扣一次内部预算。

2026-09-18 平台垫资决定（人工订单已本地实现，正式资金未入账）：指定客户一次性合计 10,000 USD、用于刷卡；资金性质为平台垫资，不是链上充值或模拟赠额。用户最新决定不设置用途或仅刷卡限制；到账后按账户统一余额、预占及现有充提规则处理。来源与原单关联仅用于账务和审计。正式凭证、科目及偿还政策待财务确认；实施与交接见[平台垫资准备](../business/platform-advance.md)。缺少数字货币入金不得补造链上交易；不得通过重复导入历史记录影响本次垫资。

## 2. 模型与标识

| 模型 | 必要职责与字段组 |
| --- | --- |
| ChannelConnection | provider、连接 ID、环境、合法实体范围、能力、凭据引用；不存前端可读秘密 |
| AccountMapping | 内部 customer/account 与来源账户/子账户的关联、资金角色、有效期、确认依据 |
| BusinessOrder | 内部业务 ID、业务类型、归属、订单维度状态；商户 orderId 仅作外部参考 |
| ProviderTransaction | 唯一来源定位、来源两层状态、当前金额/日期、脱敏来源版本、映射版本 |
| AuthorizationObservation | 有来源证据的授权金额/变化与观察时间；缺少历史就标未知 |
| FundMovement | 每项实际入账影响、所属账户、金额、业务类型、来源证据、纠错链 |
| TransactionRelation | from/to、refund/fee/reversal 等类型、证据、确认状态、有效期、确认人 |
| BalanceSnapshot | 来源账户、币种、余额类型、available/posted、computedAt、collectedAt |
| SourceEvent / Delivery | 通知身份与每次投递、验签结果、接收和处理状态、重试 |
| ReconciliationRun / Difference | 时间范围、覆盖、规则版本、证据集合、差异原因、处理记录 |

唯一键至少包含稳定数据隔离范围、channelConnectionId、resourceType、externalId。当前客户主体为 customer，不等同身份服务 tenant；新增 platform tenant 前必须单独决定模型。共享渠道账户应在连接下只保留一份来源记录，通过授权及分配关系服务多个客户，不能每个客户复制资金后求和。

业务订单可关联多笔交易；退款/费用/冲正关系独立建模。启发式匹配只能成为候选，不得进入已确认订单净额。支持部分分配时，每条关系带精确分配金额，分配合计不得超出可分配来源金额；外币退款需按对应金额维度校验，不能只比较美元退款是否超过原美元消费。

## 3. 多维状态

建议内部维度如下，具体枚举需在机器契约中定稿：

- authorization：unknown、pending_approval、held、partially_released、released、declined、expired、not_applicable。
- posting：unknown、pending、posted、failed、not_applicable。
- refund：unknown、none、pending、partial、full、failed、not_applicable。
- dispute：unknown、none、open、won、lost、closed、not_applicable。
- reconciliation：not_checked、insufficient_data、matched、unmatched、mismatch。
- sync：never_synced、syncing、fresh、stale、error、partial。

必须保存维度来源证据。`none` 要求相关范围已查全，不能由字段缺失推出；`full` 需要可信关系、可比金额与明确业务规则，不能按美元比例推算原币全退。

源交易状态和订单聚合状态分层：一笔退款失败不改变原消费已入账；争议开启不自动改变入账金额。未知或矛盾来源组合触发映射异常，但保留原始事实。

## 4. 金额、时间和汇率

新模型金额采用 `signedAmountMinor` 十进制整数字符串、`currency`、`scale`，正为流入/正余额、负为流出/负余额，零为零。币种元数据与来源单位映射必须版本化。当前 Go `amountMinor + direction` 契约不自动替换，新旧由显式适配处理。

- 数据库用有界整数或精确定点数并检测溢出，JS 使用 BigInt/十进制库；数字解析发生精度丢失后不能靠转字符串修复。
- `accountAmount`、`originalAmount`、`authorizedAmount`、`postedAmount`、退款金额和费用金额分别表达，不混用。
- `providerFx` 与 `reportingFx` 分离，包含方向、十进制 rate、来源、可知的时间、用途和证据。来源未给时间时允许未知，不凭空创建授权/入账汇率。
- 采用本系统报表折算时注明舍入策略、逐笔或汇总折算、汇率来源和规则版本，不能冒充渠道实际扣款。
- UTC 保存事件/入账/采集时间；业务查询使用指定时区，区间统一 `[from,to)`。授权日、入账日、退款日和订单归属日分开。
- 卡注销、客户升级、归属调整不得重写历史所有权；归属映射应按有效期解释。

## 5. 资金统计不变量

1. 待入账与已入账分开；同一消费由 pending 变 posted 只贡献一次入账支出。
2. 每个实际资金影响有稳定 effectKey，不能按 eventId 或 mappingVersion 新建一笔支出。
3. 来源观察记录追加保留；派生投影可版本化重建。同一来源后补修正以明确替代/纠正链处理，不能把旧值与新值都累计。
4. 费用注释、费用明细拆分与独立费用流水只有一个记账来源；返现信息不自动视为已收到返现。
5. 退款以实际入账计资金流；未匹配退款可计入账户资金流，但不能编造原订单归属。
6. 不同原币分别汇总。内部转账仅在明确合并范围且两端匹配时消除，单账户报表保留两端各自影响。
7. 订单消费净支出 = 已入账消费支出 + 已确认分配的费用 − 已确认入账退款/费用返还；其他返现或调整是否包含须单列口径。账户净资金流则包含范围内所有已确认实际入账。
8. 授权金额差不等于汇兑损益；余额类型差不等于冻结；额度不等于资金。
9. 重放、重新导入、映射升级、补同步前后，未发生来源纠正时总资金影响必须不变。

### 外币演示算例（内部假设，非 Slash 行为证明）

| 观察/事件 | 原币 | USD 已入账影响 |
| --- | --- | ---: |
| 授权观察 | CNY 720 | 0，USD 100 只是已知授权观察 |
| 消费入账 | CNY 720 | -101.00 |
| 独立费用入账 | 不与原币消费相加 | -1.01 |
| 已确认关联的部分退款 | CNY 360 | +49.80 |

账户净变化为 -52.21 USD；该订单净支出 52.21 USD，原币消费净额 360 CNY。不据此生成虚构汇率、不按授权 USD 100 重算退款、不把费用再扣一次。若只拿到最终交易，授权金额应未知。

## 6. 对账规则

对账分为来源完整性、交易关联、余额核对；分别报告，不能合成一个“成功”掩盖缺口。

只有账户、币种、余额类型、起止时点、归属和全量覆盖都可比，并确认该余额类型适用入账流量恒等式时，才可使用：

```text
预期期末已入账余额 = 期初已入账余额 + 范围内已入账有符号资金流
差异 = 来源期末已入账余额 - 预期期末已入账余额
```

信用可消费余额可能受抵押/额度等其他机制影响，未经证实不能套此式。禁止无条件跨 USD/USDT 或 cash/credit/debit 相加。`available = posted - holds` 仅能作为已验证产品模型或明确 Demo 假设。

输入不全返回 insufficient_data；不叫“少钱”。差异分类至少包含缺失记录、重复、未匹配关系、时间差、FX/费用、归属错误、来源修正、未知原因。容差按币种和政策显式配置；精确整数明细默认应精确相等，不继承通用 USD 1 容差掩盖错误。

批次记录起止快照、覆盖/失败源、规则版本、投影版本、生成时间；迟到退款/费用触发新修订版。人工匹配或处置留审核人、时间和证据，只改变调查状态，不改真实余额。

### V1 内部账本与对账补充（2026-09-07，DESIGN）

实施顺序、页面及核查差异以 [V1 账本与对账规划](reconciliation-v1-plan.md) 为准。

- **归属**：内部 customerId/cardId/ledgerAccountId 为业务身份；来源 ID 只作隔离后的关联。内部卡分户不要求上游具有逐卡资金余额。当前所属用户绑定不等于资金账户已开通或历史资金已划归该用户。
- **余额**：同客户同资产的主钱包、卡分户与其他独立子账户互不重叠，汇总行不能再加一次。逐账户保留期初与凭证；管理转入/转出在合并客户范围内净变动为零，不冒充充值/提现。
- **科目**：客户资金义务、平台资金、上游实际资产、结算应收应付及在途分别映射；不能直接断言客户余额总和等于 Slash 可用金额。正式科目及期初凭证待财务确认。
- **不变量**：同账本同资产每张凭证借贷精确平衡；账面余额可由期初和已入账分录重建。预占、风控限制、审批与实际执行分开。更正追加关联凭证，不改已入账分录。
- **跨系统去重**：人工处理、业务执行与来源补同步关联同一经济事项，重复交付不能重复入账。冻结或改绑不能重写原交易归属；后续退款根据原事项映射及已批准迁移政策归属。
- **金额显示**：USD/USDT 默认两位只是显示要求；保留原始精度，先精确汇总再显示。小于0.01的真实差额可展开、导出，不能按0.00自动忽略。OTC默认1 USDT=0.99 USD为可配置客户报价，不是利润或上游成本证明。

核对结论、作业执行状态、差异处理进度分别保存。结论归为未核对、数据不足、已匹配、有差异；逐笔未匹配与金额不符继续作为明细原因保存，与上文状态维度兼容，不能只改显示枚举丢失原因。具体机器枚举在正式契约中版本化。

内部账、业务账、外部账分别报告笔数/身份/金额/余额核对。外部可比应有 = 内部对应控制账户应有 + 有证据的有符号桥接项；未解释差异 = 来源实际 − 可比应有。桥接项必须有原记录、方向、期限和负责人，不能是配平插值；已解释在途仍需跟踪。

覆盖不全、期初未知、时点不一致、分页截断、来源失败均不能显示全量对平。相反差异不能相互抵消，多个检查命中同一事项也不能重复报告损失。复核关闭保留原核对结论；金额纠正需独立审批、凭证和新批次核对证据。

服务端固定范围、日切时区、账本截点、来源观察及规则/映射版本，明细、汇总与导出使用同一版本。迟到数据生成新修订，不静默覆盖已出具报告。财务日切需配置确认，不能由页面UTC显示或API报表默认时区推定。

当前独立 Demo 账本仅可复用机制；正式切换另需期初验证、账户映射、经济事项去重切点、唯一写入方及回滚方案，禁止新旧系统同时扣同一客户余额。

## 7. 前端职责

交易列表显示来源、原币/记账金额、状态维度和数据时点；详情显示观察时间线、真实事件证据、关联、费用及差异。缺失历史不补造完整流程图。

资金页按账户/币种/类型分组；渠道失败显示不可用或上次快照时间，不能显示为零。零、未知、不适用、不支持和获取失败采用明确不同文案；汇总由后端计算，分页列表不参与全量总计。

## 历史 2026-09-07 本地卡片执行实验

用户授权范围内新增隔离ca_*双边账本，内部资金归属、风控/自助冻结、审批与执行独立。本地测试资金来源为有对手分录的期初权益；旧卡预算不迁入、不作为扣款依据。真实Slash写能力仍未接通。具体状态、权限、科目和限制见 [卡片管理](../frontend/card-administration.md)。

## 2026-09-17：用户钱包与多卡分户本地实现

[Blnk 接入](../integrations/blnk.md) 实现独立 shadow 钱包、逐卡及逐卡在途分户。钱包转卡预占采用钱包转入在途的双边分录，成功/失败分别转入卡或退回钱包；未知不释放。用户按币种汇总时每笔资金仅计一次，清算控制账户排除。外部已确认消费允许显露卡分户负余额，主动钱包转卡禁止透支。仅最终入账事实映射到分录；未接卡授权占款，影子可用金额不得用于真实消费执行。

## 线上测试资金补充（2026-09-18，LOCAL）

008 测试额度与 009 资金订单独立于真实投影、原 transactions 和 Blnk。可用测试余额=全部期初额度+全部已完成变动−待审/处理中/结果未知提现的本金与费用。每币种变动与测试对手方配平，不跨币种求和。USD 使用 2 位、USDT 使用 6 位小数；接口金额为整数字符串。固定测试汇率、向上取费/向下取到账、状态机和授权边界详见 [测试资金流程](../business/online-test-funds.md)。这些规则不构成真实渠道执行资格。

## 项目共用钱包与客户分户（2026-09-18 用户确认）

APEXIS Op 为项目上游共用钱包；目前已有卡及对应历史记录计划分配给 ediyanghk@gmail.com，后续新用户的卡按 cardId 显式分配到各自客户。主钱包余额不复制成客户余额，也不成为测试钱包余额。来源查询按连接、父账户、虚拟账户与卡归属隔离，切换不改变交易金额或资金历史。配置实施状态见 [项目钱包流程卡](../business/project-wallet.md)。

## 2026-09-18：开卡支付规则（LOCAL隔离闭环）

开卡钱包以USD最小单位支付开卡费+首充，先进入订单在途分户；发卡确认后费用计入费收入，首充转卡分户，渠道限制核验通过才完成。发卡失败释放全额；已发卡但首充失败且确认仍受限，仅退首充，原卡补充首充不再次收开卡费。未知结果保持资金状态并核查，不能按超时退款。版本化声明随订单原子保存。此开卡账本不混入online_test或shadow，也不把内部卡分户与渠道资金池重复计算。见 [FLOW-CLIENT-ISSUING-001](../business/client-card-issuing.md)。

## 隔离 Cregis 与 OTC（2026-09-18）

[资金流程](../business/cregis-funds.md) 使用独立 shadow namespace。充值固化回执唯一日志核验后按网络/合约/交易哈希/日志索引去重；真实观察不自动归属或入账。提现本金与固定费一并预占，单名有权限且 MFA 运营审批、禁止自审；待审可取消，失败/拒绝释放，未知持续预占。

OTC 两方向独立价格，费用包含在价格中；无库存管理、无库存门槛、无人工审批。买入向下取整，零拒绝；报价到期/改价/已消费拒绝，受理后锁定快照。两币种为同币种清算分录，买入 escrow 全部完成前不可消费，部分成功恢复原 reference。价差不直接记利润，清算头寸不代表真实法币兑付能力。

## 正式资金准备规则（2026-09-18，全量未启用）

四流程见[资金中心](../business/funds-center.md)。正式 live namespace 与 online_test/shadow 隔离，钱包和新卡从零创建，测试额度及非零存量卡不迁入。开户审核通过且服务 active 后，客户首次进入充值页才请求 Cregis 地址，客户和网络持久绑定，刷新复用。充值仅按持久来源事件及对应链最终性入账。

OTC 报价 60 秒，后台双向价格独立配置，不检查平台库存；必须预占卖出资产且双资产对手账可追溯。网络提款和卡充提费用显式配置，null 禁止、0 免费，从来源另扣。卡转出扣除授权占用与在途，并在渠道降额后再次核对，未知不放款。消费与退款按来源经济键幂等。正式渠道与对账验收尚未完成，代码构造器不等于已启用。

## 人工出入金本地实现（2026-09-18）

人工资金首期只接受 USD cents 字符串，不附加用途或来源消费限制。平台垫资/线下到账批准后清算分户转钱包；出金先钱包转预占，复核后垫资回收结算，线下付款等待人员提交真实付款结果凭证。拒绝、取消、确定付款失败以释放分录退回；未知结果保留占用并查原单。2026-09-19按用户“单一授权”决定，人工资金允许同一有权运营申请和审批，仍分别记录动作和人员；关键字段不可在原单修改，全额冲正另建关联单。

余额查询按同 namespace 的 journal 汇总钱包、预占/在途与已映射卡分户，不将内部划转重复计入合计；它不是上游余额实时对账，也不涵盖未登记历史卡。人工凭证唯一约束只覆盖人工订单，尚无银行自动核验和跨系统经济事项去重。应收科目、实际平台资金及偿还政策仍待核验。实现、恢复与上线边界见[人工资金流程](../business/platform-advance.md)。

### 限定充值验收

TRC20小额验收独立于全量正式资金认证：固定客户和原Cregis地址，累计最大1 USDT；额度按已创建充值订单的本金累计，包括处理中订单，并在客户事务锁内检查。重复链上Transfer复用订单，不重复占用或入账。超额来源事实保留待处理，不能截取部分金额伪造到账。仅最终确认的USDT主网收据可创建入账订单；Blnk确定完成才显示posted。费用本批须显式配置为0。首次正式账户绑定必须零余额；重试不重新清零。见[限定验收流程](../business/deposit-address-integration.md)。

## 地址绑定与资金执行分离

真实渠道地址可在账本启用前持久绑定，但不得据此宣称已开放充值入账。唯一客户/网络创建任务在外部请求前持久化；未知结果不重新申请。导入已有地址须核实客户alias原始回执及项目归属，回调验签成功仅保存观察事实。见[地址流程](../business/deposit-address-integration.md)。

## 卡片状态与资金的边界（2026-09-18）

卡片当前状态通过渠道回查更新，独立于不可变导入历史；同步不得更改内部客户归属、资金余额或账务流水。同步异常保留最后核验值并标记异常/待核实；无新事件不按时间自动判过期，404不等于销卡。通知时间不作为卡片版本；同一来源的回查串行执行。每卡10美元仍为指定用户的目标当前可消费额度，状态同步不代表已设置该额度。实施见[卡片同步](../business/card-state-sync.md)。

### 生产展示与测试隔离

生产展示仅接受live账本，历史测试额度不重命名为正式余额。首页、总览、详情读取同一正式数据范围；尚未创建币种钱包显示未开通，账本核对异常不显示可用金额。只读展示不得自动取得资金执行资格。生产关闭test-wallet/test-funds接口，历史数据原样保留在隔离范围。见[生产展示流程](../business/funds-center.md)。

## 正式资金分能力接入（2026-09-18）

用户取消试点1 USDT上限；保留最终性、去重、双资产记账与零期初。初始双向成交价0.99、提款/卡费0，后续后台可改且旧报价失效。费用配置不开放未核验的渠道。

## 卡片详情资金展示（2026-09-19，本地）

钱包可用、本卡可用、授权预占和充提在途分开展示，不把预占或在途再次加到可用资金。充提在途取正式账本escrow的heldMinor，并按原card_transfer订单归属关联到该卡；核对不明返回未知。授权预占缺少完整且近期核验时返回未知。只读展示不开放资金执行，来源卡与资金卡按服务端精确映射关联，不使用尾号、限额或最近5笔订单推算余额。

2026-09-19展示补充：全站USDT金额固定显示两位小数，直接向零截断、不四舍五入；精确汇总后再格式化。输入、配置编辑、接口、存储、计算及原始审计/导出保留原精度，显示0.00不代表源值为零，不用于免除费用或判定对平。见[展示标准](../frontend/ui-theme.md#usdt-金额展示2026-09-19本地未部署)。本地未部署。

## OTC通知边界（2026-09-19，本地未部署）

消息中心仅订阅原订单有效状态变化，通知/阅读不修改金融状态。金额、费用、汇率取当时保存的订单快照；completed沿用原记账完成条件，unknown提示查询原单，失败不推断资金已经释放。原子outbox与投递隔离规则见[消息中心](../business/message-center.md)。


## 统一 USD 开卡增量（2026-09-19，本地）

统一开卡复用 Blnk 资金中心 USD 主钱包；订单预占、费用、卡分户通过既有 ledger 的精确金额、幂等与恢复处理。主钱包到卡分户为内部划转，不计新增收入，不重复计算资产。订单固定原 namespace 和账户，历史退款不随配置改变。旧钱包余额只允许可审计迁移分录；未知余额、归属和授权占用不得直接迁入。详见 [开卡统一流程](../business/client-card-issuing.md#blnk-与账户映射)。
## 2026-09-19 卡片历史与指标

新增只读卡指标：近30天按已核实卡类别的posted/settled负金额统计，退款单列；渠道可消费额度不得称为可提现余额；历史与通知不写资金账本。完整口径见[卡指标](../business/card-metrics.md)。

2026-09-19 额度数据接入（本地实现，未部署）：浅灰额度卡使用同一渠道快照的 availableMinor（剩余可消费额度）、cycleSpendMinor（本周期已用）和 totalLimitMinor（明确的单卡 utilizationLimit 总额度）。总额度仅提取已识别的单卡规则，卡组共享或其他规则显示未提供；只有总额度等于已用加剩余且大于零才显示进度，不用近30天消费或钱包余额推算。卡分户及统一 USD 钱包余额独立读取，保留核对状态与充提权限门槛。刷新沿用 metrics-sync，只排队只读查询；近30天消费明细区间保持。

## 非消费资金记录（2026-09-19）

[统一资金记录](../business/fund-records.md)以业务金额分项展示充提、兑换、人工出入金和开卡收费。本金、费用、退回分别关联同单，预占/释放仅为处理证据；内部划转与首充退回不算外部收支，消费退款留在卡交易。金额字符串与状态来源保持不变，读取不写账本。


2026-09-19 人工资金正式模式增量（已部署）：USD人工订单可经独立开关接入现有正式namespace；沿用最终充值证据和账本健康门槛，不创建期初或重放原充值。批准、线下付款确认、记账完成仍为独立状态；API恢复任务复用原幂等reference，不新增银行/链上付款能力。见[启用与回退](../business/platform-advance.md)。

2026-09-19 manual-funding policy supersession: the user explicitly removed the review stage. New credits proceed directly to accounting; debits reserve then proceed, while offline payouts require separate evidence of actual payment. Original pending orders are not automatically swept into execution. The authorized 1,000 USD platform advance must retain its original order and effect reference, with exactly one journal credit; no reviewer identity is fabricated.

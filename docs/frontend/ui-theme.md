# 界面主题与组件约定

更新日期：2026-09-07。客户端和运营后台共用 [MUI 主题](../../packages/shared/src/theme.ts)，公共组件位于 `packages/shared/src/components`；官网另有 PublicTheme，不能假定全站使用同一页面样式。

- 主色：微软蓝 `#0078D4`，深色 `#005A9E`，浅色背景 `#EFF6FC`。
- 蓝色用于主操作、链接、导航选中态、标签页、复选框和键盘焦点。成功、警告、错误继续使用语义状态色。
- 保留 Public Sans 字体，通过 MUI Typography variant 控制字号和字重。
- 优先复用现有 PageHeader、FiltersCard、DataTableCard、StatusChip 等组件；业务表格需要自定义列或路由交互时使用 MUI Table、TablePagination、TableSortLabel。
- 导航使用 ListItemButton，内容面板使用 Paper variant="outlined" 或 Card，表单使用 TextField、Select、Checkbox，分栏使用 Tabs。
- 颜色使用 primary.main、primary.dark、text.secondary、divider 等主题 token；避免在页面写固定品牌色。
- 通用圆角、边框、表头、悬停、焦点和表单样式在主题 components 中维护；页面 sx 仅补充布局和业务需要的样式。

当前项目已有 MUI 与 Minimals 风格公共组件，没有引入新的 UI 库或商业模板。

## 组件选型优先级

用户指定的组件目录：https://minimals.cc/components 。后续页面优先采用该目录展示的组件与交互模式，并统一使用本项目微软蓝主题。

1. 优先复用仓库中已有的公共组件。
2. 使用已安装的 MUI / MUI X Community 组件完成表格、筛选、分页、标签页、表单和反馈。
3. 按业务需要引入 Minimals Extra 对应组件或依赖，例如表单向导、上传、图表和 Snackbar；避免一次性引入整个目录。

官网展示目录不代表当前仓库已包含全部组件源码。Minimals 自定义组件接入时需先确认本地源码和依赖；缺少源码时，优先使用 MUI 实现相同交互，并如实标明来源。

## 历史 Demo 工作台与资金中心图表

以下描述保留的 Portal 原型，不是当前 ClientHome 的生产图表或余额模型。V1 已移除团队/子账户产品入口，旧聚合结构仅作历史参考。

- 使用已安装的 ApexCharts + react-apexcharts，按需加载，并由 MUI 主题提供配色和字体。
- 消费与退款趋势来自 `state.entries`，仅统计已完成 USD 消费及退款，按整数分累加；失败交易、卡片充值与 USDT 记录不混入。
- 7 / 30 天窗口截止于当前演示记录的最新日期，日历日期保持来源值；无记录日按 0 显示，界面明确该范围不等于完整账单。
- USD 组成包含账户可用、卡片余额、子账户独立余额、账户冻结 / 预占、卡片转回在途。USDT 仅包含可用与冻结 / 预占。待入账充值不计入，币种之间不汇总。
- 卡片状态环图按张数统计。环图下方列示金额 / 张数与百分比，趋势图提供可展开的数据表。无有效数据时显示空状态。


## 币种图标（2026-09-19，本地）

资金总览与 OTC 买入/卖出统一使用 `CurrencyLogo`：USD 显示美国国旗，USDT 保留 Tether 标志。法币图标采用对应国家/地区标识；尚未配置的币种显示币种代码，不默认套用美元符号。图形为本地 SVG，不依赖远程图标请求；本批不新增币种能力。

## 交易列表参考图样式（2026-09-19，本地未部署）

客户端卡交易列表/单卡交易与运营后台正式交易列表统一白底、细分隔线、56px 基础行高、32px 商户图标和“卡片交易”副标题。长内容可增加行高，窄屏保留横向滚动。已结算使用浅绿标签，待处理使用浅橙标签与灰色行文字，拒绝/失败使用浅红标签与红色行文字；灰色保持可读对比度。商户图标不降低透明度，未知品牌保留原回退。共享 `transactionVisuals.ts` 管理标签样式；现有状态判断、金额格式、授权和接口不变。

FLOW-TX-VISUAL-001：本次只调整已有只读流程的呈现，基线 main `14fe215` 加已有未提交工作。客户端 `/portal/transactions`、单卡详情交易 → 交易详情 → 关联卡片；后台 `/transactions` → 交易抽屉 → 卡片详情，沿用现有 connection、记录 ID 与返回上下文。接口链仍为 liveGet → client-api/card-projections 或 admin-api/channel-projections 网关 → Go 查询与授权 → 已导入投影；没有新增来源或跨端写入，客户归属、后台 MFA 与渠道授权继续由原服务检查。分页、搜索、失败重试、未知状态与来源时间规则保留，无待定业务政策。

本次两端类型检查、构建及交易/商户/抽屉相关测试通过；客户端浏览器使用隔离合成数据核对布局。后台真实登录、多状态浏览器流程及真实渠道未复验，未部署；全量测试结果见当前状态。此视觉验收不代表完整业务或金融验收。

### 交易详情状态精简（2026-09-19，本地未部署）

客户端详情不再展示独立“入账状态”行；两端详情的摘要及“详细状态”统一为中文状态标签与语义色图标：结算绿色、待处理橙色、失败/拒绝红色、退款/撤销蓝色、未知灰色。详情标签及悬停不拼接英文状态码；已有入账/详细状态仍参与显示判断，接口、导出原值与金额计算不变，冲突继续显示待核实。后台既有入账时间口径不变。本批为视觉精简，未部署。

2026-09-19 原币金额文案精简（本地未部署）：两端交易列表、详情及复用金额格式的页面去掉“来源最小单位”后缀；CSV 同步移除该重复列并将展示列命名为“原币金额”。保留既有数值与已验证币种格式，不新增币种缩放或换算，不修改接口原值。

2026-09-19 交易整行交互（本地未部署）：客户端交易列表、单卡交易记录和后台正式交易表格支持点击整行打开原详情抽屉，悬停高亮与手型光标；卡片/账户链接与操作按钮保留各自行为、不重复触发行点击。客户端可 Tab 聚焦行后按 Enter/空格，后台网格聚焦单元格后按 Enter 打开详情。原筛选、分页与返回上下文保持。

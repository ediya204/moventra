# 客户端页面布局与导航

2026-09-19 发布补充：本批客户端布局及近30天商户展示已随 `63371c7` 发布；迁移、测试、平台版本和验收限制见[统一发布记录](../../deploy/2026-09-19-consolidated-ui-release.md)。下方较早的“本地未部署”段落保留原实现阶段证据，不代表本批当前部署状态。

2026-09-19。各批设计、实现与发布边界分别记录如下；首页新增 FLOW-CLIENT-HOME-002 为本地候选，未部署。沿用 [UI 主题](ui-theme.md)。

## FLOW-CLIENT-LAYOUT-001

| 项目 | 范围与依据 |
| --- | --- |
| 目标及范围 | 正式客户工作台全部页面族：首页、资金及订单、卡片及开卡订单、交易、消息、帮助、设置、账户和安全。官网与 DEV Demo 不改版。 |
| 基线 | Moventra main 14fe215；已有 ClientHome、ProductionWallet、CardOverview、CardSnapshots、CardIssuing 等未提交工作，逐项兼容。 |
| 页面关系 | `/portal` → 七项主导航；资金 `/portal/funds/{deposit,fiat,withdraw,exchange,history,manual}` → 各自订单详情；卡片 `/portal/cards` → `/cards/:id`、`/cards/new`、`/card-orders/:id`、`/issued-cards/:id`；设置 → `/accounts`、`/security`。保留既有 URL 和查询上下文。 |
| 业务身份 | 沿用 session 个人 customerId；订单/卡片 ID、connection、revision 仍来自现行接口。不增加客户归属关系。 |
| 数据依据 | 现有 Go 与正式账本、渠道授权投影。浏览器验收使用本机合成响应，与真实渠道验收分开。 |
| 接口链 | ClientHome/子页 → liveGet、cryptoRequest、manualRequest、issuingRequest → client-api 同域网关 → Go 原 handler 与客户授权 → 原存储。仅整理展示与导航，不增加 API 或资金执行。 |
| 状态及操作 | 原开户、能力、报价、预占、审批、执行及 MFA 条件继续有效；未知/失败不显示零。消息与工单未接通时明确说明，可访问已有查询和帮助。 |
| 跨端变化 | 不修改后台业务流程和持久化；共享的资金导航、容器及安全页嵌入方式保持默认行为兼容。 |
| 权限 | session、客户所有权、运营 MFA 及服务端裁剪不变；本批不触碰生产权限。 |
| 验收 | E01–E03：七项导航、子页及深链、返回上下文；E06：失败与重试；E08：列表筛选及分页；手机/桌面溢出、标题和导航选中。E04/E05/E07/E09 沿用现有回归，本批无后端/写契约变化。 |
| 待定决策 | 无新增业务政策。生产发布与真实本人登录验收未执行。 |

## 布局约定

- 一级页面仅一个主标题，说明按页面任务编写。标题、分区导航、内容按顺序排列。
- 分区用可横向滚动的导航，取消页面中央的孤立大链接及重复操作条。2026-09-19按用户最新要求移除“人工出入金”独立导航入口，已有记录、查询接口和兼容详情链接保留。
- 分区用可横向滚动的导航；人工出入金纳入资金分区，取消页面中央的孤立大链接及重复操作条。
- 数据列表保持宽幅；金额表单设置合理最大宽度；长 ID、地址可换行，表格只在容器内滚动。
- 首页显示钱包与近期记录；交易页仅保留卡片交易查询、筛选与导出，移除近期资金交易区，资金历史通过链接进入。
- 设置集中账户、安全与开户状态；消息与帮助用明确能力说明和可用路径，不渲染重复通用占位卡。
- 加载、空结果、服务失败与功能未开放分别表达；二三级页面仍能通过 URL 恢复并返回。

## 本批验证

- 设计与本地实现：完成，基于 main 14fe215 的共享工作区，保留并行代码。
- 自动化：`NODE_OPTIONS=--experimental-strip-types pnpm test`，149 项通过；包含资金导航去重、详情页主导航归属、设置入口、原金额/权限/待确认请求回归。最后的标题与导航调整后，相关 24 项回归再次通过。
- 类型与构建：`pnpm typecheck`、`pnpm build` 两端通过；后台图表包仍有原有大 chunk 提示，不影响构建。
- 浏览器：真实组件 + 本地合成身份/读取响应，21 条路径 × 1440/390/320 像素共 63 次页面检查通过。每页仅一个 h1，无整页横向溢出，无运行异常；另补人工订单、充值通知详情、兼容资金入口、卡交易详情在桌面/手机下共 8 次检查通过。资金、充值、设置、交易、安全、开卡页面另作截图检查。
- 交互：手机抽屉导航、设置至安全、资金详情返回保留筛选参数、切换 ERC20 清除地址、故障后重试、空记录提示共 6 类场景通过。补充 320 像素下资金记录、人工记录、提款、安全分区的当前选中项可见性检查通过。合成地址不可用于转账；所有预览写请求拒绝。
- 真实渠道、生产数据库、本人登录：未执行。本批没有部署或金融操作。
- 预览入口：仓库根目录运行 `node tests/frontend/client-layout-preview.mjs`，打开 `http://127.0.0.1:8865/portal/funds`；仅监听本机。


## FLOW-CLIENT-TX-QUERY-001：交易筛选与导出（2026-09-19，本地未部署）

| 项目 | 本次范围与实现 |
| --- | --- |
| 目标、基线 | 客户在 `/portal/transactions` 筛选消费/退款、下钻详情并导出；main `14fe215` 加已有未提交工作。保留并行改动，仅本地实施。 |
## FLOW-CLIENT-TX-QUERY-001：交易筛选与导出（2026-09-19，已部署）

| 项目 | 本次范围与实现 |
| --- | --- |
| 目标、基线 | 客户在 `/portal/transactions` 筛选消费/退款、下钻详情并导出；源码 `e030d88`，基于 main `5d9dc2c`。独立提交并发布。 |
| 页面关系 | 主标题 → 卡片交易工具栏 → 描述直接输入框/日期与状态弹层 → 表格/分页 → 原交易抽屉/关联卡详情 → 返回。移除最近交易与数据来源展示；资金历史保留 `/portal/funds/history` 链接。 |
| 身份、数据 | 现有客户 ID、connection、revision、交易 ID 不变；沿用已授权连接（URL 指定或首个可读连接），本批不跨连接聚合，不扩充授权。 |
| 接口链 | `CardSnapshots` → liveGet/cardSnapshotContract → client-api 网关 → Go channel 查询及客户授权 → 现有投影；复用已有 keyword、detailedStatus、from、to、page、revision 参数，不变更服务端契约。 |
| 筛选、恢复 | URL 存 keyword/status/from/to，筛选变化重置第 1 页；描述为商户匹配或精确尾号/ID；描述输入框桌面宽 380px、手机占满一行，回车或点击搜索应用；状态单选，可搜索选项；日期 UTC，两端包含所选日期，API 截止次日零点不含。非法日期阻止列表/轮询请求；空结果可清空，详情关闭保留条件。 |
| 导出 | CSV UTF-8 BOM，按当前筛选从第 0 页每页 20 条读取，最多 5,000 条；固定 revision，检查 total、重复 ID、短页和失败；所有页面成功后才生成文件。显示进度，可取消；切换客户/连接/筛选或卸载取消后续处理。无残缺 CSV。 |
| 金额、安全 | 使用现有精确字符串金额格式，不转换浮点；原币未知缩放保留原始数值，界面/CSV 不显示“来源最小单位”后缀或独立列，缺失显示未知，保留入账/详细状态原值。CSV 公式开头转义，尾号强制文本、原币金额以带币种文本导出，保留前导零与大整数。无真实金融写入。 |
| 跨端、权限 | 本批改客户端交易页，后台行为不改；每次导出分页继续服务端权限检查；没有放开管理员接口或新增生产权限。 |
| 验收 | 单元覆盖跨页、版本/总数变化、重复、短页、超限、取消、网络错误、金额和 CSV；组件覆盖组合条件、分页、清空、非法日期、原详情返回。桌面/390px 浏览器使用合成身份，检查状态/日期弹层、URL、CSV 生成及无整页溢出。 |
| 待定、边界 | 状态按现有接口单选，无待定业务政策。导出是当前查询投影，不代表完整渠道账单；真实登录/渠道与部署未执行。 |

验证：首轮 159 项全量前端/网关回归通过，最终布局调整后 28 项交易/导出/工作台相关回归、类型检查及客户端构建通过。末次全量复跑停留在同时变更的 crypto-funds/customer-funds 测试进程，未产生最终汇总，已终止该次复跑；不声称最终工作区全量重验通过。浏览器检查只覆盖本地合成数据，不追认为真实渠道验收。
| 待定、边界 | 状态按现有接口单选，无待定业务政策。导出是当前查询投影，不代表完整渠道账单；真实登录/渠道未复验；两端已部署。 |

验证：发布版 156 项前端/网关回归、两端类型检查与生产构建、Worker dry-run 及文档检查通过。此前浏览器检查覆盖本地合成数据，不追认为真实渠道验收。两端线上入口与交易代码资源已和本次构建逐字节核对，版本及回退见[发布记录](../../deploy/2026-09-19-transaction-ui-release.md)。

## FLOW-CLIENT-HOME-002：首页概览与常用操作（2026-09-19，本地候选）

| 项目 | 范围与依据 |
| --- | --- |
| 客户目标 | 首先找到充值、兑换、充值到卡和开卡入口；识别钱包可用余额、卡片额度与近期消费；发现近期异常并下钻查询。 |
| 基线与身份 | main 0bf6483 的独立工作树增量；沿用 session customerId 与原客户授权，不修改共享工作区。 |
| 页面与操作 | `/portal` 顶部四个常用入口 → 原资金/开卡页；钱包横向概览 → 消费分析（宽趋势图与辅助对比）→ 紧凑卡片列表 → 最近资金记录；卡片链接保留 connection 与 back=/portal；资金订单链接保留订单 ID。手机依序单列、操作两列。 |
| 数据及接口链 | ClientHome → ProductionWallet → GET `/client-api/v1/customers/:id/crypto?limit=5`；CardOverview → GET `/client-api/v1/customers/:id/card-projections` → 各连接 `/cards?page=0&revision=...` → 原网关、授权及投影。无新增 API、迁移或渠道请求。 |
| 信息层次 | 复用本地 USD/USDT 资产图标；钱包、近期交易及卡片标题使用统一线性图标，额度使用金额符号、30天消费使用日历统计图标，均为本地 SVG 并对读屏隐藏；交易按类型显示流向图标；卡片显示状态、可消费额度与近 30 天消费。首页隐藏连接名称，保留预览数量与完整列表入口。 |
| 状态与完整性 | 钱包保持精确金额和核对状态；卡指标复用现有 metrics 格式化，零、未提供、同步未完成分别显示。只有所有连接读取成功才显示授权卡总数；每连接最多预览 5 张，不合计局部消费。 |
| 提醒与异常恢复 | 仅统计最近 5 条订单中 failed/rejected/unknown 的记录，明确范围并进入首条异常详情；不代表全账户待办。保留加载、空态、部分读取失败与重试；切换客户或重试取消旧响应。 |
| 权限与资金边界 | 四个快捷入口仍受开户状态控制；只是进入现有流程，原报价、审批和执行门禁不变。钱包与卡片额度不相加；无真实金融写入。后台不改。 |
| 验收与待定 | 设计与实现完成，无新增业务政策；160 项前端/网关测试通过，涵盖金额精度、越权响应、异常提醒、缺失/零/不完整指标、返回上下文。桌面 1280px、手机 390/320px 合成数据视觉检查通过，手机无整页横向溢出，浏览器未记录 error。首页进入卡片详情并返回工作台已实际点击核验。客户端类型检查与生产构建、文档检查通过；真实登录、渠道及生产部署本批未执行。 |

预览：仓库根目录运行 `node tests/frontend/client-home-preview.mjs`，访问 `http://127.0.0.1:8868/portal`。使用真实客户端组件和合成身份/金额，仅监听本机，订单等业务写入拒绝；预览数字不是客户真实资金。

### 首页图表增量（2026-09-19，本地候选）

- 增加状态分布堆叠条、近30天已入账消费横向对比。范围与下方预览一致：每连接最多5张；明确提示范围，不称全账户排名。
- 同币种、精度和完整统计起止区间才共用比例尺；消费降序，金额使用原精确格式，条长按 BigInt 相对最大值计算。零显示金额而无填充；负值、非法金额/区间与不完整数据不入图，并说明排除数量。不同状态包含其他/未知，读取失败分组不入图。
- 图表卡名可进入详情并返回首页；金额和状态数量为可读文本，图形不依赖颜色传意。不增加 API 请求、轮询、持久化或真实资金操作；本节为横向对比；后续每日趋势见下节。
- 161项回归通过，新增图表分组、未知状态、失败来源、完整性、零及大数比例验证。桌面1280px及手机320px图表视觉检查通过，手机无整页横向溢出；客户端构建与文档检查通过。预览样本为合成数据，生产未部署。

### 每日消费趋势（2026-09-19，本地候选）

首页新增逐卡每日消费柱状图，可切换当前预览卡片、点击或键盘聚焦日期查看精确金额。仅支持现有USD分口径；按UTC日汇总，沿用卡片完整30天窗口，首尾日明确为部分日期。读库接口复用 `/card-projections/:connection/transactions?cardId=...&metric=spending&from=...&to=...&page=...`，首次记录revision，后续分页固定版本和总数，最多5000条。切卡、刷新或卸载丢弃旧响应并停止后续读取，不新增渠道采集或周期轮询。

校验完整覆盖、卡归属、posted/settled、负金额消费、日期范围、去重和精确总额；全部分页成功且合计等于卡片metrics.spendingMinor才显示每日数据并补零。失败、缺失、金额不一致或记录过多提示恢复路径，不绘制残缺趋势。退款、费用、待入账不参与。

163项回归通过，覆盖UTC跨日、零、重复、卡片不匹配、窗口外日期、汇总不一致和分页完成前不绘图。浏览器合成数据已验证日期点击及不完整卡切换提示；真实渠道与生产未执行。

### 图表加入后的信息层级重排（2026-09-19，本地候选）

以客户任务安排页面顺序：常用操作、资金概览、消费分析、卡片管理、近期资金记录。取消左右独立长列；钱包压缩为横向两币种概览，趋势图与卡片分布/对比进入同一分析分区（桌面约1.65:1）。卡片列表独立铺满内容区，桌面统一列标题、行内状态与金额，手机保留逐项标签；删除重复的全部卡片入口，仍可使用管理卡片。手机按同样阅读顺序堆叠，不缩小成双栏。

ProductionWallet通过内容插槽容纳卡片分析区域，沿用单次钱包读取及原刷新周期；CardOverview沿用同一批卡片读取，统计口径、异常处理、归属、执行门禁与详情返回不变。163项回归通过；桌面1280/1440及手机390/320合成数据检查，320px无整页横向溢出。未部署。

2026-09-19（本地未部署）：账户设置移除“个人设置”和“我的账户”导航及中转页，侧栏设置直达 `/portal/security`；`/portal/settings` 重定向安全页，`/portal/accounts` 保留无入口的旧深链查询。安全页只有密码、邮箱和2FA设置，无单项重复标签。见[流程与验证](../business/two-role-login.md#flow-auth-security-001账户安全设置2026-09-19本地未部署)。


### FLOW-CLIENT-HOME-MERCHANT-001：消费前五商户（2026-09-19，本地未部署）

| 项目 | 本批范围 |
| --- | --- |
| 目标与基线 | main b77c222 加现有工作区；仅替换首页右侧卡片状态/消费对比，保留并行改动。 |
| 页面关系与身份 | `/portal` 消费分析 → 左侧选择预览卡片 → 右侧展示该卡近30天消费前五商户及金额；沿用客户、连接、卡ID，原卡片详情返回路径不变。 |
| 数据与接口链 | CardOverview / SpendingTrend → liveGet → client-api 原网关 → Go 已授权卡交易投影；与每日趋势共用已有完整分页及metrics窗口，不新增请求或接口。 |
| 金额口径 | 当前所选卡片、USD、UTC、posted/settled消费；不含退款/费用。按来源描述原文（仅去首尾空格）合并，用BigInt求和、降序取五名，不推断品牌同一性；缺失显示“商户未提供”。不是全账户商户排名。 |
| 状态与恢复 | 加载、无消费、不完整、请求失败分别显示；所有分页及金额核对成功才显示排行。切卡/刷新丢弃旧响应，重试沿用左侧入口。 |
| 跨端与权限 | 后台不改；沿用服务端客户与卡片授权；无写操作、资金影响或新授权。 |
| 验收与待定 | 9项首页专项通过，覆盖精确大额、同名合并、前五截取、未知商户、零消费、分页/汇总不一致拒绝；客户端类型检查及构建、边界检查通过。无待定政策；浏览器及真实渠道未复验，未部署。 |

2026-09-19 同批补充：商户排行榜复用共享 MerchantLogo，每行展示32px品牌图标；未识别商户显示默认店铺图标，加载失败显示品牌首字母，保留Logo.dev署名。品牌匹配仅用于图标，不改写来源名称和统计分组。

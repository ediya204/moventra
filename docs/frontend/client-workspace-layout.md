# 客户端页面布局与导航

2026-09-19。本批为本地 UI/UX 优化，未部署。沿用 [UI 主题](ui-theme.md)，保留工作区内已有首页、卡片中心和状态图标改动。

## FLOW-CLIENT-LAYOUT-001

| 项目 | 范围与依据 |
| --- | --- |
| 目标及范围 | 正式客户工作台全部页面族：首页、资金及订单、卡片及开卡订单、交易、消息、帮助、设置、账户和安全。官网与 DEV Demo 不改版。 |
| 基线 | Moventra main 14fe215；已有 ClientHome、ProductionWallet、CardOverview、CardSnapshots、CardIssuing 等未提交工作，逐项兼容。 |
| 页面关系 | `/portal` → 七项主导航；资金 `/portal/funds/{deposit,fiat,withdraw,exchange,history,manual}` → 各自订单详情；卡片 `/portal/cards` → `/cards/:id`、`/cards/new`、`/card-orders/:id`、`/issued-cards/:id`；设置 → `/accounts`、`/security`。保留既有 URL 和查询上下文。 |
| 业务身份 | 沿用 session 个人 customerId；订单/卡片 ID、connection、revision 仍来自现行接口。不增加客户归属关系。 |
| 数据依据 | 现有 Go 与正式账本、渠道授权投影。浏览器验收使用本机合成响应，与真实渠道验收分开。 |
| 接口链 | ClientHome/子页 → liveGet、cryptoRequest、manualRequest、issuingRequest → client-api 同域网关 → Go 原 handler 与客户授权 → 原存储。仅整理展示与导航，不增加 API 或资金执行。 |
| 状态及操作 | 原开户、能力、报价、预占、审批、执行及 MFA 条件继续有效；未知/失败不显示零。消息与工单未接通时明确说明，可访问已有查询和帮助。 |
| 跨端变化 | 不修改后台业务流程和持久化；共享的资金导航、容器及安全页嵌入方式保持默认行为兼容。 |
| 权限 | session、客户所有权、运营 MFA 及服务端裁剪不变；本批不触碰生产权限。 |
| 验收 | E01–E03：七项导航、子页及深链、返回上下文；E06：失败与重试；E08：列表筛选及分页；手机/桌面溢出、标题和导航选中。E04/E05/E07/E09 沿用现有回归，本批无后端/写契约变化。 |
| 待定决策 | 无新增业务政策。生产发布与真实本人登录验收未执行。 |

## 布局约定

- 一级页面仅一个主标题，说明按页面任务编写。标题、分区导航、内容按顺序排列。
- 分区用可横向滚动的导航；人工出入金纳入资金分区，取消页面中央的孤立大链接及重复操作条。
- 数据列表保持宽幅；金额表单设置合理最大宽度；长 ID、地址可换行，表格只在容器内滚动。
- 首页显示钱包与近期记录；交易页以查询为主，不再重复钱包卡片。
- 设置集中账户、安全与开户状态；消息与帮助用明确能力说明和可用路径，不渲染重复通用占位卡。
- 加载、空结果、服务失败与功能未开放分别表达；二三级页面仍能通过 URL 恢复并返回。

## 本批验证

- 设计与本地实现：完成，基于 main 14fe215 的共享工作区，保留并行代码。
- 自动化：`NODE_OPTIONS=--experimental-strip-types pnpm test`，149 项通过；包含资金导航去重、详情页主导航归属、设置入口、原金额/权限/待确认请求回归。最后的标题与导航调整后，相关 24 项回归再次通过。
- 类型与构建：`pnpm typecheck`、`pnpm build` 两端通过；后台图表包仍有原有大 chunk 提示，不影响构建。
- 浏览器：真实组件 + 本地合成身份/读取响应，21 条路径 × 1440/390/320 像素共 63 次页面检查通过。每页仅一个 h1，无整页横向溢出，无运行异常；另补人工订单、充值通知详情、兼容资金入口、卡交易详情在桌面/手机下共 8 次检查通过。资金、充值、设置、交易、安全、开卡页面另作截图检查。
- 交互：手机抽屉导航、设置至安全、资金详情返回保留筛选参数、切换 ERC20 清除地址、故障后重试、空记录提示共 6 类场景通过。补充 320 像素下资金记录、人工记录、提款、安全分区的当前选中项可见性检查通过。合成地址不可用于转账；所有预览写请求拒绝。
- 真实渠道、生产数据库、本人登录：未执行。本批没有部署或金融操作。
- 预览入口：仓库根目录运行 `node tests/frontend/client-layout-preview.mjs`，打开 `http://127.0.0.1:8865/portal/funds`；仅监听本机。

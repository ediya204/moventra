# 已有后台模块恢复与查询发布

日期：2026-09-13（UTC+08:00）。本批已同步 GitHub 并发布正式卡片、账户、渠道查询；不代表全部旧本地模块已接通正式服务。

## 实现与生产范围

- 正式 `/cards` 恢复全部卡片查询、分页、名称/尾号/状态筛选及详情往返；详情可以查询此卡交易。数据来自现有授权渠道投影，保留来源和导入时间。
- 正式 `/customers` 按现有 `accounts:read` 客户授权查询账户并分页。`/system/channels` 展示授权连接和采集/导入时间，关联卡片及交易。
- 卡片查询保留管理员角色、MFA、连接授权、审计和数据隔离；没有新增资金操作。
- 原 Node/SQLite 管理、费率、BIN、渠道配置、资金、审批、报表等实现与测试，以及 Python 只读采集代码，恢复至 `services/local-workspace`。这些代码可在独立本地环境运行，未部署为正式业务写服务。
- 正式菜单将这些已有但尚未迁入的功能标为“待迁移”。后续仍需完成正式身份、权限、存储与分模块流程验收，不能只解除菜单禁用。
- 本批未连接生产数据库进行数据操作，未执行迁移、旧数据导入或真实渠道金融写操作。旧库包含演示记录；迁入范围尚待用户确认，原库保持不变。

## GitHub 与部署

- 正式运行源码：`eeb9d48c466df51cdc36e371a0165f8d0f937a7a`。
- 后续本地管理会话及 DEV 页面恢复：`682dccb81681fe321dc38c464b843d34e88cdcb3`。
- 后续 Python 采集器恢复：`aa98f1c7b15a963c58acbb6a5ab43498bbdd70a0`。后两次提交不改变正式 API 源码或后台生产产物；重新构建的正式产物摘要相同。
- Render API：`dep-daipejrm8hqs73dpluj0`，固定运行源码如上，2026-09-13 02:18:22.858882 +08:00 为 `live`。
- 前次同源码候选 `dep-daip8tjm8hqs73dp375g` 构建成功但停留在更新阶段，已取消后重试。没有确认该次停滞的根因；期间原 API 保持就绪。
- 后台 Worker `moventra-admin`：`f2299a9f-2a32-4c21-8ff9-ef23d02f29f7`，入口 https://admin.moventra.me/admin/login 。
- 客户端本批未部署，保留 `a1f68672-b2ea-473d-9546-e8e085593b94`。
- Render 自动部署关闭、preDeployCommand 为空，本批未改变这些设置。

## 本次验证

- 前端/网关 70 项、隔离本地服务 136 项、Python 模拟客户端 33 项测试通过。
- 隔离 PostgreSQL 全套 Go race 测试通过，包含卡片跨页、名称/尾号/状态查询、卡关联交易、不适用参数拒绝、MFA/跨连接授权及审计失败拒绝。
- Go vet/build、两端 TypeScript/Vite 构建和后台 Wrangler dry-run 通过；仍有既有图表大分包提示。
- 本地浏览器验证登录、独立管理会话、总览、BIN 列表至详情、费率列表；使用合成数据，没有执行资金操作。
- 线上 19 项健康、登录路由及匿名权限 HTTP 检查通过；另核对三个新增页面返回 200、匿名卡片 API 返回 401。
- 线上 HTML 和六个关键 JavaScript 文件与本次产物 SHA-256 全部一致，包含卡片、账户、交易、布局及开户页面。
- 真实账号登录/MFA 后的业务浏览器验收、真实 Slash 调用及旧数据迁移未执行。HTTP 200 和匿名 401 不代替登录后的业务验收。

## 回退与后续

应用回退目标：API `dep-daionse7bikc739gslqg`；后台 Worker `cc1bffe4-f93b-4aa1-a0d7-0092a1b914bc`。本批没有数据库迁移，不需要为应用回退改写业务数据。

各模块现有能力、迁移差异及验收流程见 [已有后台功能恢复记录](../business/existing-modules-restoration-2026-09-13.md)。BIN、费率、财务等正式接通仍未完成；旧业务数据是否迁入及排除演示记录的范围仍待确认。

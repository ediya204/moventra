# 全站业务文档整理记录

日期：2026-09-07。用户范围为整理全站业务逻辑、更新 Markdown、上传 GitHub；本轮不变更业务代码、不部署、不运行真实金融动作。

## 核对基线

- 初始源码：`d9a5d40932b149ac866b12fc3cb30b816bdd8ed0`；收口前纳入 `0d5158d` 的正式渠道投影与商户标识源码，以及 `2918584` 的部署结果记录，重新核对受影响文档。
- 规范工作区：`moventra` 中已上传内容及额外未提交业务改动；以文件内容比较判断，不用较旧本地 main 的状态推断发布结论。
- 本地服务：旧 `adsflow-admin-react/demo-server/slash` 中的管理、卡片、归属、产品、Portal、资金、日期与同步处理源码。服务及私有数据没有迁入本仓库。
- 此前概览运行版本：admin Worker 与 Render Go 使用 `a42e2b9`，详见 [既有记录](../../deploy/2026-09-07-operations-overview.md)。渠道 [独立记录](channel-projection-2026-09-07.md)已确认新版本 Go/两端 Worker 部署，保留本人登录业务验收未完成的边界。本轮没有重新执行其中的迁移、导入、部署或认证验收。

## 整理结果

业务总入口为 [全站业务逻辑](../business/README.md)，专题分为身份与开通、卡片交易、资金运营、正式路由与接口。各流程记录入口、角色、数据归属、状态、金额影响、来源、更新方式及交付边界。

本轮纠偏包括：

1. 正式运营入口已含 `/workbench`，安全设置保留 `/session?security=1`。
2. 官网咨询只下载本地文件，注册预览不创建身份；Go 用户注册、主体开通、资金服务和运营授权互相独立。
3. 正式 Go、网关和前端白名单各有范围；客户列表网页默认前 50 条，渠道列表服务端每页 20 条，两者不能混称全量。
4. 开户审批、数字货币审批和旧 Portal 订单查询分别处理；三套资金模型不能混合余额或费用。
5. 当前 OTC 固定版本报价与旧 Portal 有效期报价分开；费率方案配置没有自动接入实际 OTC 计费。
6. DEV 日期筛选默认 30 个 UTC 自然日且结束日含全天，正式渠道起止日期默认不限且截止不含；运营日图使用香港时间。刷新不触发上游采集或导入。
7. 已上传的正式基础卡资料、商户标识与本地未合入的五标签工作台、解冻申请分开；文档不附带业务代码。
8. 来源调查与计划中的三层资金对账分开；历史测试数字保留为历史，不自动宣布草案已验收。
9. 渠道投影需要 staff 身份、MFA 与独立连接授权；手动导入不混入客户交易/概览，不开放客户数据归属或金融执行。

## 本轮检查及边界

本轮进行源码与路由/状态的静态核对、Markdown 相对路径与片段检查、文档内算例检查、UTF-8/格式及提交清单检查。公开 Slash 文档只核对本次引用的交易、卡片/产品和通知语义；不是租户 API 验证，详细 URL 及边界见 [卡片专题](../business/cards-and-transactions.md)。

检查结果：17 份 Markdown，328 处仓库相对链接、1 处标题片段、1 个 JSON 示例均通过；100 USDT 扣 0.5% 费用后按 0.99 兑换为 98.50 USD，以及香港/UTC 日界算例通过。执行本地校验脚本 `python3 /tmp/validate-moventra-business-docs.py` 和 `git diff origin/main --check`，无断链、冲突标记或格式错误；脚本仅用于本轮核对，未作为业务源文件上传。

未重跑前端、Go、隔离数据库或资金自动化；此前测试与发布只通过对应记录引用。未读取私有数据库、真实数据快照或凭据，未调用真实渠道、修改身份授权、迁移数据库、冻结/解冻真实卡或转移资金。

提交只包含 Markdown。构建产物、业务源文件、运行数据、秘密和其他任务的未提交改动均不属于本轮上传范围。OpenAPI `servers` 的旧“未部署”文案已登记，JSON 未在本轮改动。

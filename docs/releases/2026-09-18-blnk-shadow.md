# Blnk 集成代码发布（2026-09-18）

用户授权同步、部署。发布快照基于 origin/main `717cd43`，仅合入本次 Blnk 代码、测试和文档。共享工作目录其他未提交工作保留。

## 发布行为

- 新增 Blnk 适配、用户钱包/多卡/在途分户、可恢复记账、精确金额和只读账本契约。
- 默认 `LEDGER_MODE=disabled`；生产继续使用原查询投影与权限。受认证的账本读路由在账本关闭时返回 503 `ledger_disabled`，无凭据返回 401；不新增公共记账写入口。
- `shadow` 当前仅可连接本地隔离数据库和本地 Blnk。生产部署的是集成代码；Blnk 云服务与真实记账尚未激活。
- 新迁移编号为 `006_blnk_shadow.sql`，避免覆盖已发布 004 角色迁移。001–005 字节不变，迁移入口仍须显式调用。此次不执行生产迁移；006 仅在隔离测试库验证。
- 钱包转卡仍需未来真实 Slash 调额适配及一致性验收；Cregis/链上验签、最终性、卡授权占款与期初迁移均未接通，不改变真实客户资金。

## 本次验证

- 独立发布目录冻结锁文件安装、两端 TypeScript 和构建、82 项前端/网关回归通过。
- 官网 SEO/联系入口及构建产物补充 17 项检查通过；两端 Wrangler production dry-run 通过。
- Go vet/build 通过；真实 Blnk 0.15.4 容器 + 隔离 PostgreSQL 全套 race 回归通过，包括已有角色/开户/目录测试及新增账本测试。Firebase 真实身份测试未配置，不代表本次真实登录验收。
- 既有后台大包构建提示仍在，不阻塞构建。

## 回退基线

- Render API：`f981baaea0cc79b25f97e475ee411e9433fba0bb` / `dep-daiq1jvqj5pc73b4pldg`。
- 客户端 Worker：`d6a19345-d429-4aab-9492-6746f68a3641`。
- 后台 Worker：`a710e132-86f5-4b0b-9a20-a9312d25eb93`。
- 此次不更改生产数据库结构和账本模式，回退代码无需撤销资金分录。

实际提交、部署版本和上线探测结果将在本文件末尾追加。

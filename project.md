# Moventra 项目说明

更新日期：2026-09-18。本文以 GitHub `main` 提交 `812e5ae` 为整理基线，并对照本地开发规范与未提交方案，汇总项目定位、开发思路、实施次序和能力边界。发布状态引用仓库已有记录，本次文档编写未重新验证线上服务或真实业务流程。

## 项目定位

Moventra 是面向客户与运营人员的账户、卡片、交易查询及业务管理平台。官网、客户端、运营后台与 API 共用一个仓库，各自独立构建；共享认证、界面组件和数据契约。

- 正式仓库：[ediya204/moventra](https://github.com/ediya204/moventra)，统一维护 `main`。
- 官网及客户端入口：[moventra.me](https://moventra.me)。
- 运营后台入口：[admin.moventra.me](https://admin.moventra.me)。
- ADSFLOW 是独立历史项目；迁入的部分源码不代表两者运行环境、数据或发布状态相同。

当前产品同时包含正式身份与查询、隔离本地业务流程、线上测试资金流程及影子账本实现。必须按模块判断运行模式，不能将模拟资金、只读投影或审批结果当作真实支付与结算。

## 仓库结构与技术栈

| 路径 | 职责 | 主要技术 |
| --- | --- | --- |
| `apps/client` | 官网、客户登录与客户工作台 | React 18、TypeScript、Vite、MUI |
| `apps/admin` | 运营登录、客户目录、业务查询与审核界面 | React 18、TypeScript、Vite、MUI |
| `services/api` | 身份校验、主体授权、业务接口、审计及受控 CLI | Go、PostgreSQL、Firebase Admin SDK |
| `services/local-workspace` | 隔离本地业务服务与合成数据预览 | Node.js、SQLite、Python 采集工具 |
| `packages/shared` | 两端共用认证、UI、主题、类型及数据适配 | TypeScript、React |
| `packages/assets` / `packages/tooling` | 品牌资源与构建配置 | 静态资源、Vite |
| `deploy` | Cloudflare、Render 与 Blnk 配置及发布记录 | Workers、Docker |
| `tests/frontend` / `docs` | 前端回归、业务规范、契约与验收记录 | Node 测试、Markdown、OpenAPI |

主要请求链路：浏览器 → 同域 Cloudflare 网关 → Go API → PostgreSQL。Firebase 提供登录身份，Go 校验角色、客户归属、资源权限与运营 MFA；前端隐藏入口不能代替服务端授权。

## 能力与验收边界

以下是基线源码及版本记录的摘要，不是本次重新执行的验收报告。旧 README 的早期描述存在滞后，涉及新增能力时优先核对对应专题及源码。

| 模块 | 基线包含的能力 | 仍需区分的边界 |
| --- | --- | --- |
| 身份与开户 | 客户/管理员角色、Firebase 登录、运营 MFA、开户申请及审核相关流程 | 身份认证、审核与服务激活分别判断；管理员也不自动获得所有客户数据权限 |
| 客户与运营查询 | 账户/交易查询、运营概览、注册用户目录、渠道卡交易及客户卡片快照 | 卡片测试快照与内部绑定不代表真实资金归属或上游卡片控制权 |
| 线上测试资金 | 独立测试额度、模拟充值、兑换、提现、订单及运营审核 | `mode=online_test`；真实执行及提现资格为 false，不调用真实资金渠道 |
| 本地业务工作区 | 用户、费率、BIN/渠道、卡片归属、审批及资金原型源码已迁入 | 使用隔离服务及合成数据，不作为生产资金后端 |
| Blnk 与后台任务 | 影子账本、持久化任务、Worker、观测和恢复工具 | 本地实现及基础设施发布不等于生产账本激活；线上测试资金不接 Blnk |
| 渠道接入 | 独立来源投影、项目钱包及逐卡归属、普通 Webhook 收件与来源观察 | 通知处理完成不代表前端投影更新、资金记账或全量历史同步 |
| 供应商与 BIN | 正式目录、来源产品映射、客户覆盖 → 客户组 → 产品默认的三级定价、独立权限及审计 | 目录发布记录为 8 个草稿产品、暂停供应商、商业价格未配置；不开放真实发卡、首充或 issuing-worker |

最新[线上测试资金记录](docs/business/online-test-funds.md)记载了 API、两端前端和选择性迁移的发布结果，但真实客户登录及运营 MFA 后的下单/审核人工验收尚未完成。自动化测试、资源发布及未认证请求被拒绝不能替代该验收。

## 核心开发思路

以下归纳现有规范与本地方案，不新增业务政策或生产授权。

1. **以完整业务流程交付。** 开发前填写 `FLOW-xxx` 流程卡：用户目标、页面层级、主体及记录 ID、数据权威、接口链、状态、权限、异常恢复和验收。只读流程覆盖入口 → 筛选列表 → 详情 → 关联对象 → 返回；处理流程还需覆盖客户发起、后台处理、客户查结果及审计。
2. **先确认事实与契约，再接页面。** 来源事实、内部派生、内部设计及待核实能力分别标记。接口 schema、shared transport、同域网关、Go handler、授权与存储一起核对；前后台读取同一事项，不各自维护权威状态。保持旧接口兼容，避免以宽泛代理绕过缺失接口。
3. **渠道与内部业务分层。** Slash 提供上游对象和交易事实；客户归属、内部卡身份、定价、审批及审计由 Moventra 管理。适配器隔离渠道字段，外部 ID 按连接与资源类型定位。来源观察、查询投影、业务事项和资金影响各自可追溯，不用显示字段推断金融事实。
4. **客户、卡和上游钱包分开。** 一个客户可有多卡，按明确 cardId 维护归属，不按卡名、尾号或 cardholder 姓名归并；上游共用钱包余额不是某个客户的余额。项目钱包范围与逐卡归属须同时满足，未绑定数据不能自动开放给客户。
5. **资金模式和权威保持明确。** 只读投影、隔离 Demo、`online_test`、Blnk shadow 和未来真实执行分别管理。金额使用精确值及明确币种/单位；审批、预占、执行、渠道确认、入账和对账分开。未知结果先查询恢复，不能通过重发新指令、删流水或改余额解决。
6. **界面按可恢复流程组织。** 列表、二级和三级详情有稳定 URL，返回保留筛选/分页；加载失败、无权限、空数据、未知和零值分别表达。提交成功以服务端确认为依据，超时重试复用幂等标识，列表、详情和待办按同一结果刷新。
7. **先做可验证的小闭环。** 复用已有认证、权限、账本恢复和组件，逐条补齐断链；先在隔离数据中验证，再按授权进入真实渠道。大数据查询使用服务端筛选、稳定分页、覆盖声明和有界采集，未实测不承诺容量或全量同步。

资金核对按内部账、业务逐笔、上游可比资金池三层推进。缺少期初、币种、日切、归属或来源覆盖时标记数据不足；消费限额和内部预算不能作为真实资金执行依据。正式账本接管另设切点、唯一写入方、重复记账防护及恢复方案，见 [V1 账本与对账规划](docs/domain/reconciliation-v1-plan.md)。

## 本地与 main 的差异登记

本次开始时原共享目录 `HEAD=ce0a88a`，落后远端 `812e5ae` 57 个提交，且保留大量未提交文件；不能把该目录所有内容视为远端增量。本文以隔离工作树中的远端源码核对，原目录的修改时间不作为版本先后依据。

| 主题 | 已核实的状态与开发处理 |
| --- | --- |
| BIN 目录与开卡 | main 已有独立 issuing 模块与正式目录管理；目录上线不代表真实执行已启用。现有发布记录注明未部署 issuing-worker、未授予金融写权限；后续分别验证目录、报价、订单、执行及渠道结果 |
| 随机卡名与默认 holder | 本地方案要求从 100 个英文姓名中随机命名、同一订单重试不重新命名，Slash 使用渠道默认 holder。远端基线仍用订单生成卡名并传入客户 holder，因此本项是待独立合入和验证的本地增量，不能写成线上能力；历史卡不批量改名 |
| 项目钱包与客户归属 | main 已包含项目钱包范围和逐卡归属机制，见 [项目钱包方案及证据](docs/business/project-wallet.md)。旧整批测试快照、项目钱包归属和后台显示必须按版本核对；仍在其他任务处理的显示问题不在本文追认为已修复 |
| Slash 连接环境 | 复用既有已放行环境。main 目录发布记录包含 Render 真实只读成功证据；本地白名单记录中的 MCP 失败只适用于那次调用链。本文未重新调用渠道，不据此要求重复配置，也不宣称所有出口均已复验 |
| 数据库迁移 | 旧工作区与 main 已出现迁移编号含义差异，例如旧本地 `007_card_issuing.sql` 与 main 的客户快照迁移不可互换。后续以已部署版本及 checksum 重新设计增量，不覆盖已应用迁移，不从旧目录整体发布 |
| 文档状态 | 早期“未实现”、后续 LOCAL 记录及发布记录并存。引用具体版本、环境与日期，优先核实源码和专项记录；历史测试不能改写成本次通过 |

## 后续实施顺序

以下是依据现有缺口整理的建议队列，不等于已执行或新增授权；尚未分配的负责人标记为待分配。

| 优先级 | 下一步 | 完成条件与边界 | 确认角色 |
| --- | --- | --- | --- |
| P0 | 收敛工作区与版本差异 | 在最新 main 隔离工作树逐项识别已合入、待合入及冲突文件；保留其他任务改动；迁移编号及 checksum 一致 | 开发维护者（待分配） |
| P1 | 补齐已发布查询、测试资金及 BIN 目录的登录验收 | 客户会话与运营 MFA 下验证列表/详情/刷新/返回、跨端同一事项、越权拒绝及失败重试；测试资金始终明确模拟 | 开发及验收人员（待分配） |
| P1 | 合入获批本地体验与业务增量 | 随机卡名/default holder 等逐项核对最新契约与兼容性，再运行对应组件、API、幂等和权限测试；不扩大真实发卡范围 | 开发维护者（待分配） |
| P2 | 完成来源 → 归属 → 影子事项 → 核对的闭环 | 事件去重、乱序/重复/金额变化复核、持久化任务恢复、双端读取一致；保持覆盖不足可见，不宣称全池对平 | 开发及财务（待分配） |
| P3 | 真实渠道执行准备 | 确认产品资格、默认 holder、限制语义、未知创建恢复、真实到账证据、Blnk 运维与资金池核对；实证与授权满足后才开放相关能力 | 渠道运营、财务及用户确认 |
| P4 | 生产账本接管与规模化 | 明确期初和切点、唯一写入方、历史去重、备份恢复、回滚与负载指标；独立评审及授权 | 财务及平台维护者（待分配） |

## 本地开发

前端使用 Node.js 22+、pnpm 10.32.1；本地业务工作区要求 Node.js 22.13+。Go 模块声明版本为 1.26.5，API 本地环境要求 PostgreSQL 17+。具体配置见各子项目 README，凭据只通过本机或运行环境注入。

以下命令已按本文 main 基线核对。原共享目录尚未收敛版本时，不应直接假定它具备相同脚本；优先在最新 main 的独立工作树中运行。

在仓库根目录运行：

```bash
pnpm install --frozen-lockfile
pnpm dev:client       # 默认 http://127.0.0.1:8853
pnpm dev:admin        # 默认 http://127.0.0.1:8850
```

前端启动不自动配置 Firebase、创建客户主体或启动 Go API。API 的独立开发库、环境变量及启动步骤见 [Go API README](services/api/README.md)，默认端口为 `8870`。迁移命令必须针对明确选定的隔离开发库，不能直接套用到生产数据库。

需要合成数据的本地业务预览时，单独运行：

```bash
pnpm workspace:dev
```

该命令启动隔离 API `8868` 与后台 `8850`，不要与前述后台开发进程同时占用端口；端口冲突按服务错误处理，不停止其他会话的进程。详细行为见[本地工作区说明](services/local-workspace/README.md)。

## 验证与交付

根据改动范围选择验证命令，以下为开发入口，本次文档更新未运行这些业务测试：

```bash
# 仓库根目录：前端边界、测试、类型及独立构建
pnpm check:boundaries
pnpm test
pnpm typecheck
pnpm build:client
pnpm build:admin

# 本地业务服务回归
pnpm test:workspace

# 仓库根目录：新建隔离 PostgreSQL 测试库的 API 回归
bash services/api/scripts/test-postgres.sh

# 在 services/api 目录执行
go test ./...
go vet ./...
go build ./cmd/...
```

普通 Go 测试缺少对应环境时会跳过数据库或 Blnk 集成项，应以输出为准。涉及影子账本、Worker 或恢复流程时，按专项文档执行对应隔离测试。页面存在、构建通过或 HTTP 200 均不代表业务闭环完成。

官网/客户端和运营后台分别通过 Cloudflare Worker 发布；Go API 使用 Render，PostgreSQL 保存应用数据。当前 `render.yaml` 将 API 自动部署设为关闭。GitHub 提交、迁移、API 发布、两端前端发布及认证后的业务验收分别记录；文档推送本身不执行部署。

## 开发约束与后续工作

- 保留共享工作区无关改动，仅提交本次范围内文件；不提交凭据、真实客户数据、本地数据库或构建产物。
- 金额明确币种、最小单位、精度与方向；未知、零、失败、不支持分别表达。不同币种不直接相加。
- 来源记录、投影、资金影响及审计需可追溯、幂等；审批与结算保持独立。
- 前后端变更按完整流程验收：入口、列表、详情、关联对象、操作结果、异常重试及跨端一致性。
- 生产数据库连接、迁移、部署及真实金融写接口均需独立明确授权。

后续优先补齐已发布测试资金流程的认证浏览器验收，再按业务授权推进真实渠道验证、生产账本边界及运行保障。企业能力、渠道执行与完整资金对账等目标，应以专题的实际实现及验收证据逐项确认。

## 继续阅读

- [开发约束](AGENTS.md)、[开发总纲](docs/DEVELOPMENT.md)、[业务闭环标准](docs/business/delivery-standard.md)
- [文档索引](docs/README.md)、[业务总览](docs/business/README.md)
- [客户端](apps/client/README.md)、[运营后台](apps/admin/README.md)、[Go API](services/api/README.md)
- [交易与资金规则](docs/domain/transactions-and-funds.md)、[接口契约](docs/api/contract.md)、[资金验收场景](docs/testing/financial-scenarios.md)
- [客户卡片绑定](docs/business/customer-card-binding.md)、[项目钱包](docs/business/project-wallet.md)、[线上测试资金](docs/business/online-test-funds.md)
- [BIN 目录流程](docs/business/bin-catalog-sync-2026-09-18.md)、[BIN 发布证据](deploy/2026-09-18-bin-catalog-sync.md)、[Webhook 接入记录](deploy/slash-webhook-online-2026-09-18.md)
- [Slash 与 Blnk 方案](docs/integrations/slash-blnk-plan-2026-09-18.md)、[来源持久化设计](docs/integrations/slash-persistence-design-2026-09-18.md)
- [Blnk 接入](docs/integrations/blnk.md)、[运行与恢复](deploy/blnk/monitoring-and-recovery.md)、[部署记录](deploy/README.md)
